# Recipes refresh and standard catalog window — 2026-09-06

Recipes now shares Recruitment's compact rows, shaded pixel equipment icons, recessed panels and fixed launch footer. The brief leads with purpose and inputs; instructions/checks, tools/skills and customization/share actions remain available in disclosures. All 101 built-in recipes resolve to themed SVG artwork; custom recipe artwork retains its existing fallback.

Both dock doors now mount their catalog through StationUI.toggleTerm with the standard wide tier. The shared manager owns the title, close/draft guard, drag, keyboard/pointer resize, minimized strip, focus and stored geometry. The old viewport overlay and duplicate scanline layer are removed. Recipe launch/schedule/create and class-selection deep links retain the existing context and handlers.

Live proof in the seeded app on port 9164: category filtering and search; recipe setup; minimizing/restoring an entered draft; keyboard resize and header drag; tab titles; coexistence with Abilities; close/reopen through Recipes and Recruit dock entries; all six theme colors; no native control paint; narrow roster/brief/back navigation and setup width; close/resize controls within viewport at 145% text. The setup was not submitted to a paid model.

Evidence is in the worktree's ignored `.uishots-recipes/`: `live-check.log`, `layout-check.log`, `recipes-final.png`, `recipe-setup-final.png`, and `recruitment-final.png`. Updated text-size regression enforces shared window geometry/glass; existing recipe, class-loadout, icon catalog, identity and resize checks passed. Full-suite/integration receipt follows in qa/STATUS.md. No installer rebuild or deployment is included.
